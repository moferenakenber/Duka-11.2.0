Commands and Notes


docker ps -> shows what's up and running
docker-compose ps -> if at a location with no docker related file -> "no configuration file provided: not found" if not then results.

-- Dockerfile & docker-compose.yml -- files are needed to make one.

docker-compose up --build -> reads the docker-compose.yml file.
docker-compose up -d -> "d" means detached from the terminal and so you can close the terminal.

docker exec -it duka_app php artisan migrate:fresh --seed



- tmux
 1977  tmux
 1978  tmux ls
 1979  tmux a -t 1

Split Vertically	Ctrl+b, let go, then % (Shift + 5)
Split Horizontally	Ctrl+b, let go, then " (Shift + ')
Close a Pane	Ctrl+b, let go, then x, then y
Exit Entirely	Type exit and hit Enter (works just like a normal terminal)


- btop for docker

sudo docker stats
sudo ~/.local/bin/lazydocker
